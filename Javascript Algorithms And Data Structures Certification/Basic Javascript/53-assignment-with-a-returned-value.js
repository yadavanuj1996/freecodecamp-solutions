// Example
var changed = 0;

function change(num) {
  return (num + 5) / 3;
}

changed = change(10);

var processeArg = 2;

function processed(num) {
  return (num + 2 ) + 8;
}

processed = processArg(7);
